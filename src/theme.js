import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#6D67E4",
    },
    secondary: {
      main: "#EF6A2D",
    },
    otherColor: {
      main: "green",
    },
  },
  typography: {
    fontFamily: ["Montserrat"],
  },
});
